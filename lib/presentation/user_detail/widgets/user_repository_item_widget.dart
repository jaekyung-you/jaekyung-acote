import 'package:flutter/material.dart';

class UserRepositoryItemWidget extends StatelessWidget {
  const UserRepositoryItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
